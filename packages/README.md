# packages

Shared schemas, types, and utilities live here.

Recommended subfolders:

- `schemas/`: shared data contracts
- `shared/`: reusable runtime utilities

Keep reusable code here instead of duplicating logic across backend, crawlers, workers, and frontend.