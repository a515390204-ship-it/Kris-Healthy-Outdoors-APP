# Parsed_Text

Derived text extracted from raw files, screenshots, PDFs, or OCR workflows.

Parsed text is not source truth. It must always point back to a raw file in `_inbox/Raw_Files/` or the legacy `raw/sources/` directory.