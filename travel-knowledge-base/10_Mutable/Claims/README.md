# Claims

Claims are structured assertions extracted from Evidence.

Examples:

- a place requires reservation
- a restaurant is crowded on weekends
- a route includes several places in sequence
- a map source gives a specific address

Claims can be active, verified, contradicted, outdated, or rejected.