# workers

Knowledge pipeline workers live here.

Workers transform raw sources into Evidence, Claims, Conflicts, Cards, Views, and frontend indexes.

Recommended worker groups:

- `ingest/`
- `extract/`
- `claim/`
- `conflict/`
- `update-cards/`
- `export/`
