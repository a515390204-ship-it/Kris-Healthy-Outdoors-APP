# crawlers

Source crawlers live here.

Crawler outputs must become immutable RawSource records before any extraction or synthesis.

Recommended subfolders:

- `xiaohongshu/`
- `maps/`
- `official/`

Crawler code must not update knowledge cards directly.