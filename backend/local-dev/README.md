# local-dev

Local development scripts and temporary API prototypes live here.

Root files recommended for migration:

- `api-server.ps1`
- `static-server.js`
- `start-kb-api.cmd`
- `start-product.cmd`

These scripts should eventually be replaced by a production backend service matching `travel-knowledge-base/90_System/_api_contract.md`.