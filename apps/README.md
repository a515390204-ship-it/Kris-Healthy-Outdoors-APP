# apps

Frontend and product-facing applications live here.

Recommended subfolders:

- `web/`: web frontend and static prototypes
- future mobile/mini-program clients if needed

Frontend apps consume knowledge through backend APIs and generated datasets. They should not mutate `travel-knowledge-base/` directly.