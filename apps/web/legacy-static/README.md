# legacy-static

Temporary home for the current root-level static prototypes.

Files to migrate here:

- `index.html`, `styles.css`, `script.js`
- `outdoor.html`, `outdoor.css`, `outdoor.js`
- `kb.html`, `kb.css`, `kb.js`
- `kb-manage.html`, `kb-manage.css`, `kb-manage.js`
- `xhs-admin.html`, `xhs-admin.css`, `xhs-admin.js`

Keep files together because they currently use same-directory relative links.

After migration, verify:

- homepage loads
- outdoor demo loads
- knowledge base demo loads
- xhs admin demo loads
- local API fallback still works
