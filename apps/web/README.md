# web

Web frontend layer.

Current root-level static prototypes should be migrated here after deployment paths are verified.

Suggested structure:

```text
apps/web/
├── legacy-static/   # current static HTML/CSS/JS prototypes
└── src/             # future frontend app source
```
